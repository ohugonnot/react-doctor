import SectionTitle from "../compenents/UI/SectionTitle";

const Empty = (props) => {

    return (
        <>
            <SectionTitle title={"Erreur"} message={"404 Bitch"}></SectionTitle>
        </>
    )
}

export default Empty